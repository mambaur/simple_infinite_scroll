name: simple_infinite_scroll
description: Infinite scroll list pagination, helps you simplify the process of displaying large amounts of data.
version: 1.1.4
homepage: https://bauroziq.com
repository: https://github.com/mambaur/simple_infinite_scroll

environment:
  sdk: '>=3.1.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

example:
  main: simple_infinite_scroll_example.dart